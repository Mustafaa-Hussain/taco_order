spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_data_db
    username: postgres
    password: toor
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
